# Photonix 环境配置（简化示例）
# 用途：复制为 .env 并按需修改；仅包含核心必配项，适合快速上线
# 部署：Docker Compose 使用 env_file: .env 注入；本机/PM2 请以进程环境注入

# === 核心服务 ===
# 服务监听端口
PORT=13001
# 运行模式（生产环境请设为 production）
NODE_ENV=production
# 日志级别（error|warn|info|debug）
LOG_LEVEL=info

# === 目录（容器内路径，需挂载持久化） ===
# 媒体库根目录（图片/视频）
PHOTOS_DIR=/app/photos
# 数据目录（数据库/缩略图/HLS 等）
DATA_DIR=/app/data

# === Redis（缓存/限流/队列） ===
# 生产（Compose 内）常用：redis://redis:6379
# 本机开发常用：redis://localhost:6379
REDIS_URL=redis://redis:6379

# === MariaDB 数据库配置 ===
# MariaDB 服务器地址（Docker Compose 内使用服务名）
MARIADB_HOST=mariadb
# MariaDB 端口
MARIADB_PORT=3306
# MariaDB 用户名
MARIADB_USER=photonix
# MariaDB 用户密码
MARIADB_PASSWORD=photonix123456
# MariaDB root 密码（用于容器初始化）
MARIADB_ROOT_PASSWORD=photonix123456
# 查询超时设置（毫秒）
MARIADB_QUERY_TIMEOUT=60000

# === 安全（必须修改为安全值） ===
# JWT 签名密钥：请替换为 32+ 位强随机字符串
JWT_SECRET=sdgjkldsjgkljkldfg445f6hf654dsgf4d4sdfvcddw
# 管理操作口令：请替换为强口令
ADMIN_SECRET=19950526

DISABLE_WATCH=true

PERFORMANCE_MODE=high

WORKER_MEMORY_MB=1024
UV_THREADPOOL_SIZE=8
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX_REQUESTS=50000000

SETTINGS_REDIS_CACHE=true

DETECTED_CPU_COUNT=11

